import { productsUtilFormatters } from './products-util-formatters';
describe('productsUtilFormatters', () => {
  it('should work', () => {
    expect(productsUtilFormatters()).toEqual('products-util-formatters');
  });
});
